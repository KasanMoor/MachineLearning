#ifndef MATRIX
#define MATRIX

#include <string>
#include "mat.h"

class MyMatrix: Matrix
{
   private:
      int k; // number of eigenvalues to pick
      int r;
      int c;
   void read(string filename)
};

#endif
